<?php
/**
 * @file
 * Code for the Dynamic Example training feature.
 */

include_once 'dynamic_example_training.features.inc';

/**
* Implements hook_ctools_plugin_directory().
 */
function dynamic_example_training_ctools_plugin_directory($module, $plugin) {
    if ($module == 'ctools') {
        return 'plugins/' . $plugin;
    }
}